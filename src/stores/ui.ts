import { atom, map } from 'nanostores';

export const isCartOpen = atom(false);
export const isSlideOversOpen = atom(false);
export const isMenuOpen = atom(false);
export const isThemeOpen = atom(false);

